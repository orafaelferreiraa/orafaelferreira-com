{
  "mcpServers": {
    "azure-mcp": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--env-file",
        "${CLAUDE_PROJECT_DIR:-.}/.azure-mcp.env",
        "mcr.microsoft.com/azure-sdk/azure-mcp:latest"
      ]
    }
  }
}
